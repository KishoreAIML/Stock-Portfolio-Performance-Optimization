from setuptools import setup, find_packages

setup(
    name = "Stock Portfolio Performence Optimization",
    version = "0.0.1",
    description = "forcasting return and risk using ml, than optimizing portfolio for given assets with high performence.",
    author = "Kishore Tirumani",
    license = "MIT",
    packages = find_packages()
)